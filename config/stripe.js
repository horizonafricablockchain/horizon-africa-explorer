// Configuration for Stripe API
module.exports.stripe = {
    // Declare all variables at the top
    api_key: process.env.STRIPE_API_KEY // Store your Stripe API key here, accessed through environment variables
};